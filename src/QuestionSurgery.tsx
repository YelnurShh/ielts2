import { useState } from 'react';
const labels:Record<string,string>={topic:'Тақырып',task:'Міндет',limit:'Шектеу',cause:'Себеп',problem:'Мәселе',solution:'Шешім'};
export default function QuestionSurgery({question,marks,onChange,disabled}:{question:string;marks:Record<string,string>;onChange:(marks:Record<string,string>)=>void;disabled:boolean}){const [kind,setKind]=useState('topic');return <div className="learning-panel"><h2>Сұрақты бөліктерге бөл</h2><p>Белгі түрін таңда да, сұрақтағы сөздерді бас. Белгіленген сөзді қайта бассаң, белгі алынады. Белгілер эссемен бірге сақталады.</p><label className="field">Белгі түрі<select disabled={disabled} value={kind} onChange={e=>setKind(e.target.value)}>{Object.entries(labels).map(([key,label])=><option key={key} value={key}>{label}</option>)}</select></label><div className="question-words" lang="en">{question.split(/\s+/).map((word,i)=><button type="button" key={i} disabled={disabled} className={labels[marks[i]]?'marked '+marks[i]:''} aria-label={word+(labels[marks[i]]?' — '+labels[marks[i]]:'')} aria-pressed={!!labels[marks[i]]} onClick={()=>{const next={...marks};if(next[i]===kind)delete next[i];else next[i]=kind;onChange(next)}}>{word}{labels[marks[i]]&&<small lang="kk">{labels[marks[i]]}</small>}</button>)}</div></div>}
